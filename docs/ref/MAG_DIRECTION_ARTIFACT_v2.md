# Mag direction artifact v2

**Commitment:** `mag-direction-artifact-v2-001`  
**Version:** 2.0.0-direction  
**As-of:** 2026-08-05  
**Status:** **Frozen direction** — supersedes proposal §7 narrative; v1 proposal stays for problem/solution depth  
**Job:** One artifact: where we've been · where we are · what's left before v3 · how v4/v5 reshape direction **using Mag's own framework**  
**Honesty:** This is planning law on disk — not a shipped product claim. Alpha until RUN A ritual green on home PC.

**Load with:** `docs/FRAMEWORK_LOAD.md` · `HANDOFF_MAG_AGENT_TODOS.md` · `docs/ref/MAG_NEXT_CODING_RUN.md`

**Parents:** `MAG_PROJECT_PROPOSAL.md` · `DNA.md` · `OPERATOR_CARD.md` · `MAG_V4_CONDUCTOR_LOOP_DRAFT.md` · `MAG_v5_PIPE.md`

---

## 1. Card (list face)

**Title:** Mag direction artifact v2  
**Blurb:** Mag is alpha software with a real constitution. v2 graduation is merge + ritual. v3 is **substrate** (orchestrator, seats, pack, Office). v4 is **mold** (process, steward, eval). v5 is **optional forest power** (GSTD, Vast train, XRPL) on the same spine — not three products.

**Bullets:**
- **Been:** v1 Grok/X strike origin · v2 repo in projects folder · v3 planning on branches
- **Are:** v2 = this harness; v3 = RUN sheet + PR #15; home merge gate still open  
- **Next:** RUN A merge ritual → RUN B factory pilot → RUN D freeze gate → CHAT wave → v4 steward — **then** call it "v3 build"  
- **v4 modifies v3:** No new loops without eval case + pattern tag; v3 features must fit conductor mold  
- **v5 modifies direction:** External stacks are **seats + spores**, not architecture; train on export, route on seat_score  
- **Framework rule:** Artifact > transcript · promote > predict · janitor first · L3 on irreversible

---

## 2. Anchors

| Field | Value |
|-------|-------|
| kind | direction_artifact |
| id | mag-direction-artifact-v2-001 |
| parent | mag-project-proposal-001 |
| ts | 2026-08-05 |
| path | docs/ref/MAG_DIRECTION_ARTIFACT_v2.md |
| operator | Nacho |
| active_branch | cursor/v3-deepseek-run-e2ce (PR #15) |
| merge_gate | PR #8 → #9 → #10 → #11 on home `main` |

---

## 3. Where we've been (filed history)

| Era | What | Framework lesson |
|-----|------|------------------|
| **v1 Grok** | Strike chord, X constitution, napkin `@2028342347361141030` | Spawned project; need disk leaf |
| **v2 repo** | `local_sovereign_agent` in projects folder | Residual DNA; this harness |
| **v3 plan** | Substrate + Chat + factory (branches #13/#15) | Fit v4 mold; FILE gates |
| **v4 draft** | Conductor, steward, eval | Process before volume |
| **v5 pipe** | GSTD, Vast train, XRPL | Optional seats, not architecture |
| **Pain filed** | Plan theater, chat≠memory, agent mess | Steward normalizes soil |

**Meta-lesson (from framework itself):** We stopped predicting the final nation. We file episodes (`MAG_BEHAVIORAL_COMPOUNDING.md`). v4/v5 names are **direction**, not promises.

---

## 4. Where we are (honest now)

### 4.1 Maturity

| Label | Truth |
|-------|-------|
| "v2 shipped" | **No** — #8–#11 not merged to home `main` |
| "v3 shipped" | **Partial** — modules on branches (#13, #15); not product-complete |
| "v4/v5 shipped" | **No** — planning + partial steward wave 1 |
| "Alpha" | **Yes** — constitution real; layman Chat still catching up |

### 4.2 On disk today (trust these paths)

| Works | In flight / branch |
|-------|-------------------|
| DNA, Office, orchestrator, agent CLI, cursor_bridge | Unified router on #8 |
| improve scout, container docs, training events schema | FKB merge #9, autorun #10 |
| PR #15: pack modes, steward-scope, DeepSeek run, v5 docs | Factory pilot not run |
| Vast **inference** path (`scripts/vast/`, providers.yaml) | Vast **train** = v5 only |
| gstdcoin index + pull scripts | Clones cloud/local; not Mag-native soil yet |

### 4.3 Three houses (zeitgeist — stable direction)

| House | Repo / network | Mag role |
|-------|----------------|----------|
| **Beads** | Mag (private office) | Custody, routing, pack, residual |
| **Forest** | mycelial-republic | Public law, fork, optional republic train |
| **GSTD forest** | gstdcoin (v5 pipe) | Optional DePIN seat — not throne |

---

## 5. What remains before "launch into v3 build"

**Do not start v3 volume until RUN A is green.** "v3 build" here means **substrate + first product waves**, not the whole backlog.

### 5.1 Hard gate — RUN A (home PC, human)

```text
Merge PR #8 → #9 → #10 → #11
mag.cmd doctor
routing_smoke.py  PASS
verkle-audit --dry
autorun --once --dry
v3_home_smoke.cmd
v3_deepseek_run.cmd   (optional proof of one agent loop)
```

**Done when:** `main` carries honest router + autorun + FKB; ritual green.

### 5.2 v3 build entry (first waves — not full backlog)

| Order | Run | Outcome | Seat |
|-------|-----|---------|------|
| 1 | **RUN B** — factory-audit-json pilot | First v4-shaped artifact chain (plan→build→audit→FILE) | DeepSeek + Cursor audit |
| 2 | **RUN D** — factory freeze gate | No `[build]` without frozen BUILD — stops plan theater | Conductor |
| 3 | **CHAT 1–4** | Preflight + status strip — Chat stops feeling dead | Cursor |
| 4 | **RUN C remainder** | skill_gate on audit (C2) if not on main | DeepSeek |
| 5 | Pull **#13 + #15** or merge to main | v3 modules + steward wave 1 on home |

**Explicitly not v3-build blockers:** L-conductor train, resonance, mobile voice, GSTD implement, XRPL tools, riddle packs.

### 5.3 Operator soil (before scaling agents)

- [ ] `.env` from `.env.example` — secrets in vault, not git  
- [ ] `memory/` bootstrapped on home PC (not cloud-only)  
- [ ] MagImproveDaily + MagVerkleWeekly registered  
- [ ] Optional: portable bag + git bundle for cold reboot  

### 5.4 One-line gate

> **v3 build starts when RUN A ritual is green and RUN B factory pilot files its first audit JSON — not when PR count rises.**

---

## 6. How v4 modifies plans (framework-native)

v4 is not "version after v3 finishes." v4 is the **mold** v3 work must fit (`MAG_V4_CONDUCTOR_LOOP_DRAFT.md`).

### 6.1 v4-first rule (applied to every v3 PR)

Before merging any v3 feature, answer four questions:

1. Which **pattern** in `training_patterns.yaml`?  
2. Which **eval case** proves it?  
3. Which **join keys** on disk (`run_id`, `task_id`, `build_slug`)?  
4. What is **auto vs promote**?

If blank → defer, even if clever.

### 6.2 v4 reshapes v3 backlog (not additive)

| v3 backlog item | v4 modification |
|-----------------|-----------------|
| L-conductor (v3-009) | Becomes **inference seat after export + eval** — not parallel roadmap |
| Spider (v3-007) | Must emit `spider_signal` → switchboard tier, not prompt-only |
| Virtual desk (v3-006) | Ops + REPORT; GUI deferred until Chat CHAT-1–4 |
| Bead export (v3-005) | **`steward-train-prep` + training-export** — v4 weekly habit |
| Tesuji Grove (v3-012) | L4 talk layer — **no grove poem without trail** (L1→L4 rule) |
| Mobile voice (v3-013) | After Chat + voice/turn API — not before Office honest |

### 6.3 v4 deliverables that constrain v3

| v4 build | Forces v3 to… |
|----------|----------------|
| Conductor phase overlay | Route declares `plan|build|audit` — BUILD frozen before build |
| Loop detectors (plan theater) | Autorun must show `drain_delta` or pause |
| Cost ledger | Every remote seat logs spend — Chat shows chip |
| Local steward jobs | Catalog, digest, spore — **cleanup without user asking** |
| Seat intelligence | Router uses playbook priors — not "pick DeepSeek because vibes" |
| Actor memory (engines) | Cursor/DeepSeek/Grok cards on disk — not chat memory |

**Direction change:** v3 **volume** shrinks; v3 **substrate** must expose hooks v4 can score.

---

## 7. How v5 modifies plans (framework-native)

v5 is **optional power on the same spine** (`MAG_v5_PIPE.md`) — not a fork of Mag.

### 7.1 v5 rule (from constitution + tiers)

| Track | Framework slot | Never |
|-------|----------------|-------|
| **GSTD DePIN** | L1-cap seat when probe ok | Default seat; bridge undeployed |
| **Vast training** | Rent GPU → export curriculum → promote weights | DNA on Vast-only disk; auto-train T0/T1 |
| **XRPL** | T2 read tools; T3 payment with human seal | Autorun spend; seeds in git |

### 7.2 v5 reshapes v4 (feeds back)

| v4 must file | So v5 can… |
|--------------|------------|
| Spore catalog (`steward-spore`, gstd/xrpl digests) | Route with knowledge > README |
| `training-export` eval green | Vast train has curriculum |
| `seat_playbook.yaml` + cost ledger | seat_score picks GSTD vs Vast vs local |
| Switchboard peer model | GSTD worker, xrpl-read, vast-train as peers |
| CHAT preflight | Show "GSTD: N nodes · Vast: configured · XRPL: read-only" |

### 7.3 v5 does not change north star

Still: **local janitor first · frontier scarce · human L3 · artifact > transcript.**

v5 adds **capacity** (GPU train, DePIN infer, ledger rails) — not identity.

### 7.4 Agent interface eventual goal (nuts to bolts)

Cursor-class UI is **viewport + preflight** on the same harness:

```text
Chat/IDE → preflight (v4 seat_score + cost)
        → route.v2 → switchboard peer
        → worker (local | DeepSeek | Cursor | cloud | v5 seats)
        → FILE trail + steward cleanup
```

Steal contracts from Cursor (`MAG_STEAL_AUTOPILOT.md`); reject SaaS memory throne.

---

## 8. Refinement — how to keep this artifact alive

### 8.1 When to amend (promote gate)

| Trigger | Action |
|---------|--------|
| RUN A green on home PC | Update §5.1 checklist; bump `ts` |
| RUN B audit JSON on disk | Mark factory pilot proven in §5.2 |
| v4 conductor eval case added | Link in §6.1 |
| v5 track starts implement | Move from `piped` to `test` in §7 |
| Major operator session | One paragraph delta in §4 — not rewrite |

**Amendment shape:** new `commitment` suffix `-002`; parent id unchanged; diff in `memory/decisions_log.jsonl`.

### 8.2 What not to do

- Do not merge v3 backlog into this doc (keep `MAG_v3_BACKLOG.md`)  
- Do not add calendar dates (framework: emergent, not predicted)  
- Do not claim shipped until ritual or artifact path exists  
- Do not let chat supersede this file — **LOAD this for direction**

### 8.3 Load order for any seat (updated)

```text
1. docs/FRAMEWORK_LOAD.md
2. docs/ref/MAG_DIRECTION_ARTIFACT_v2.md   ← this file (direction)
3. docs/ref/MAG_PROJECT_PROPOSAL.md         ← depth (problem, inventory)
4. docs/ref/MAG_NEXT_CODING_RUN.md          ← next code run order
5. HANDOFF_MAG_AGENT_TODOS.md               ← merge + ops
6. mag.cmd context-pack
7. ONE job from queue/todo.md
```

### 8.4 Success metrics (artifact v2)

| Metric | Pass |
|--------|------|
| New agent loads artifact | Can state RUN A gate + v4-first rule without chat |
| v3 PR merged | Names pattern + eval case |
| v5 mention | Described as seats, not dependency |
| Operator | Can answer "what before v3 build?" in one sentence (§5.4) |

---

## 9. Edges

| Edge | Target |
|------|--------|
| parent proposal | `MAG_PROJECT_PROPOSAL.md` |
| run sheet | `MAG_NEXT_CODING_RUN.md` |
| v4 mold | `MAG_V4_CONDUCTOR_LOOP_DRAFT.md`, `MAG_LOCAL_STEWARD.md` |
| v5 pipe | `MAG_v5_PIPE.md`, `MAG_v5_GSTD_FOREST.md`, `MAG_v5_VAST_TRAINING.md`, `MAG_v5_XRPL.md` |
| backlog | `MAG_v3_BACKLOG.md` §6 (v5 rows) |
| chat target | `V3_DEEPSEEK_RUN.md` §6 wave 3 (CHAT) |
| ilap protocol | `docs/ref/MAG_ILAP_PROTOCOL.md` · `docs/ref/templates/ILAP-PROPOSAL.md` |
| framework | `DNA.md`, `OPERATOR_CARD.md`, `MAG_BEHAVIORAL_COMPOUNDING.md` |
| releases | `docs/ref/releases/VERSION_REGISTRY.md`, `configs/releases.yaml` |

---

## 10. Closing (one paragraph)

We've been building **law and loops** while the nation stayed on branches. We are alpha with a honest map: graduate v2 on home PC, enter v3 build through factory + Chat + wiring — but **fit every slice to v4 mold** (patterns, eval, FILE). v5 adds GSTD, Vast training, and XRPL as **scored optional seats**, not a new product. Refine this artifact at phase gates; let the framework compound behavior instead of guessing the final shape.

---

*v2 direction artifact — update at RUN A green and RUN B first audit.*
