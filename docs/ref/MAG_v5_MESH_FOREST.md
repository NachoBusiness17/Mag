# Mag v5 — Mesh forest (offline comm + willing device mesh)

**Commitment:** `mag-v5-mesh-forest-001`  
**As-of:** 2026-08-05  
**Status:** **Piped** — ILAP research + local clones now; implement after v4 steward + G4 interconnect design  
**Parents:** `MAG_v5_PIPE.md` · `MESH_COMM_REPOS_INDEX.md` · `MAG_ILAP_PROTOCOL.md` · `docs/archive/ZEITGEIST.md`

**Job:** Optional **fourth forest house** — mesh/offline transport + willing household participation — on the same Mag spine as GSTD DePIN. Not a chat app. Not a throne.

---

## 0. One line

**v5 mesh = Mag routes coordination across opt-in device paths (LAN, BLE mesh, optional mailbox relay) while beads stay on disk — same switchboard law as GSTD compute seats.**

---

## 1. Vision (eventual service — your ask)

You described a **self-governing agent** greater than the sum of parts:

```text
Self-contained growth · willing user participation · devices connected
· parallel governance · bypass capture · MIT/permissive forest license (like GSTD)
```

**Mag framing (honest):**

| Your intent | Mag expression |
|-------------|----------------|
| Self-governing agent | **Conductor + steward + promote gate** — habits become law, not model whims |
| Greater than sum of parts | **Compose stolen contracts** (mesh + DePIN + beads) via switchboard — one router |
| Self-contained | **Cold vertex on each bead** — no SaaS memory throne |
| Willing participation | **Opt-in edge nodes** — GSTD serve, Briar mailbox, Bridgefy SDK in *your* app — L3 enroll |
| Devices connected | **Household mesh + GSTD swarm** — capacity layers, not identity |
| MIT distributed license | **Forest seats are permissive forks** — mycelial-republic + optional mesh/GSTD repos |
| Mycelial republic | **Many beads, no king** — mesh connects **roots**, not replaces constitution |

This is **not one monolith app**. It is a **protocol stack**:

```text
L0  beads (Mag private office) — DNA never merged
L1  switchboard + pigeonhole — coordination without chat scroll
L2  optional seats: GSTD infer · mesh transport · XRPL read
L3  human seal — enroll device, run mailbox, promote habit
L4  republic fork — public law, practice soil
```

---

## 2. Four houses (updated zeitgeist)

| House | Network | Role |
|-------|---------|------|
| **Beads** | Mag (private) | Custody, routing, pack, residual |
| **Forest** | mycelial-republic | Public law, fork, train export |
| **GSTD forest** | gstdcoin DePIN | Optional **compute** seat |
| **Mesh forest** | Bitchat/Bridgefy/Briar **contracts** | Optional **transport** seat (v5 piped) |

No house is throne. Mag routes; it does not own users' mesh radios.

---

## 3. What we steal (contracts only)

From local clones (`mine/raw/mesh_comm/`):

| Source | Contract | Mag slot |
|--------|----------|----------|
| **Bitchat** | BLE mesh → Nostr fallback routing | conductor `transport_chain` research |
| **Bitchat** | Multi-hop TTL + dedup | switchboard steer — no broadcast storms |
| **Bridgefy** | P2P / Mesh / Broadcast modes | pigeonhole transmission enum |
| **Bridgefy** | Propagation profiles (dense/sparse) | spider thresholds for crowded vs offline |
| **Briar** | Sync without central server | beads + sync-when-path-exists (already zeitgeist) |
| **Briar mailbox** | Willing relay when peer offline | optional peer seat — operator enrolls |
| **Briar** | Tor transport | tier T2 research — not default Mag path |

**Reject:** shipping Briar/Bitchat UI inside Mag; silent background mesh; GPL codebase merge into MIT Mag core without boundary.

---

## 4. ILAP now → service later

### Phase R0 (today — first iteration)

1. `./scripts/pull_mesh_comm_repos.sh`  
2. ILAP proposal per epic (`docs/ref/proposals/ILAP-v3-entry.md` § mesh)  
3. FEATURE_COMPOSE cards from `field-steal` / research-pack  
4. **No Mag code** until overlap + routing matrix green  

### Phase R1 (v4 — mold)

- `steward-mesh-digest` job — weekly contract summary from clones  
- Spore catalog rows tagged `mesh_*` in improve evals  
- training_events `research_dive` with `source: mesh_comm`  

### Phase R2 (v5 — probe)

- Read-only transport probe (LAN/BT availability — no enlist)  
- Chat preflight chip: `Mesh: LAN only` / `Mesh: not configured`  
- `MAG_MESH_ROUTE_MAP.md` — when to prefer LAN switchboard vs hub  

### Phase R3 (v5+ — willing participation service)

**Not before:** G4 interconnect spec without throne registry · v4 steward daily · operator consent UX

| Component | Behavior |
|-----------|----------|
| **Mesh agent service** | Sidecar process — Bridgefy/Bitchat-protocol **or** LAN-only Mag switchboard peer |
| **Enrollment** | L3 — explicit "this device relays" / "this device serves GSTD" |
| **Economy** | Optional — GSTD-style task credits **or** pure mutual aid (republic choice) |
| **Governance** | Fork equality — no rank token; promote amends procedure |

```text
        [User bead — Mag DNA]
               │
    willing L3 enroll
               │
    ┌──────────┴──────────┐
    │  Mesh sidecar       │  GSTD gstdbot (parallel)
    │  relay / LAN peer   │  infer edge
    └──────────┬──────────┘
               │
         switchboard peers
               │
         conductor routes
```

---

## 5. Routing doctrine (frozen intent)

```text
T0  local Ollama + disk beads        → always first
T0  LAN switchboard (:8765)          → same household, no internet
T1  mesh transport (opt-in)          → when probe ok + user enrolled
T1-cap GSTD DePIN                    → compute overflow
L2  Grok/Cursor                      → scarce
L3  human                            → enroll · promote · spend
```

**Mag wins when:** it knows **not** to mesh (no consent, wrong tier, capture pattern).

---

## 6. Gates before implement

| Gate | Check |
|------|-------|
| v2 merged | PR #8–#11 home `main` |
| ILAP mesh research | ≥5 FEATURE_COMPOSE cards filed |
| Clones local | `pull_mesh_comm_repos.sh` + manifest |
| G4 design | Interconnect without privileged registry (draft) |
| v4 steward | scope cards daily |
| License review | GPL/AGPL stay reference — no contaminated merge |

---

## 7. Links

| Doc | Role |
|-----|------|
| `MESH_COMM_REPOS_INDEX.md` | Clone list + tags |
| `configs/mesh_comm_repos.yaml` | Machine manifest |
| `MAG_v5_GSTD_FOREST.md` | Parallel DePIN track |
| `MAG_ILAP_PROTOCOL.md` | Research before code |
| `MAG_STEAL_AUTOPILOT.md` | Zeitgeist filter |

---

*v5 mesh forest — piped. Research clones + ILAP now; service after willing-participation gates.*
