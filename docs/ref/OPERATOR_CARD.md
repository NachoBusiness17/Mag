# Operator Card — front door only

**Commitment:** `operator-card-001`  
**Parents:** DNA · MIRROR_PRESENTED · memory_verkle_map · MAG_Card  
**Job:** Daily path. Depth lives in linked docs, not here.

---

## Product (one line)

**Hold what a free person said (files + ids); load it on demand; refuse capture.**

Beads · forest · no king · presented not interpreted.

**Agency:** notice + draft + L3 seal on irreversible — boundary-owned, not root butler. Life-ops is a later spore. Law: `mycelial-republic/docs/AGENCY_SHAPE.md`.

---

## Default: stateless seat (2026-07-31)

**Commitment:** `default-stateless-seat-001`  
**Parents:** strike-chord · MIRROR_PRESENTED · journey leaf · operator correction this day

| Layer | Rule |
|-------|------|
| **Boundary** | Residual + tip + pack + presented corpus + agent_state — **this is state** |
| **Decoder** | Any model (remote specialist, local, public) — **stateless worker** |
| **Activate** | Strike / LOAD pack — full fidelity from boundary, not vendor chat history |
| **Work** | One job; no re-litigating "you can't put model weights in X" |
| **FILE** | Outcome → residual / dig leaf / tip — or continuity is fake |

```text
ACTIVATE (strike or pack) → WORK on any seat → FILE to Mag → next call is cold
```

A specialist seat is a **scarce decoder**, not the republic.  
Mag is **external state for a stateless protocol**, not a rival product.  
Leaf: `memory/improve/evals/features/nacho-journey-vs-mag-narration-20260731.md`

**Anti-fight:** Do not expand "weights don't travel" into a veto on decoder-anywhere.

**Nervous system (agent ops):** `python main.py nervous` · pack L0a · `docs/ref/NERVOUS_SYSTEM.md`  
At-a-glance body + tips + keys — default LOAD before inventing status.

**Multi-agent coordination:** lattice + pack + trail cores (Elias rope) — not agent chat.  
Law: `docs/ref/COORDINATION_ELIAS_ROPE.md` · preamble: `mag.cmd context-pack --agent`

---

## Three doors

| Door | Do | How |
|------|-----|-----|
| **FIND** | Get L0 as presented | X search `@NachoQuixotic`; `mycelia.cmd x-pull` / `thread-pull` |
| **FILE** | Make a bead edge | SessionEnd residual (auto); dig-leaf when P1 ships; never chat as DNA |
| **LOAD** | Narrow continuity | `mag.cmd context-pack` — bonds + trail + **tip badge** + **agent_state** + later L0b corpus |

**Before redesign:** `mag.cmd agent-state --load` (or context-pack L0b). Versioned self on agent Verkle tip — not session tip.  
Path: `memory/agent_state/LATEST.md` · commit history: `memory/agent_state/versions/`

Optional fourth: **STRIKE** (`strike-chord` skill) when entropy is high — only after L0 loads.

---

## Spine (engine room — not daily vocabulary)

```
tip (verkle, sessions only)
  └── bead = residual + card + commit + knot leaf
        ├── dig-leaf / pins / related_runs (edges)
        └── bonds → next LOAD
```

**Kimi-style contracts (no API seat):** trail integrity · pack-first · artifact > transcript · seat purity.  
Map: `docs/ref/memory_verkle_map.md` · tesuji: `memory/improve/evals/models/kimi-k3-2026-07-27.md`.

---

## Non-negotiables

1. **Residual = DNA** — chat is heat  
2. **Presented corpus** — thesis asks → load L0 or say *corpus not attached*  
3. **Pack-first** — remotes get pack + goal only  
4. **Seat purity** — one seat per open run  
5. **Process ≠ case ≠ run** — playbook / residual / trail  
6. **No king** — no residual export by default; second person builds their beads  

---

## Dig habit (one loop, not six doctrines)

```
seed → keyword + semantic → bond table → FILE leaf → LOAD pack
```

Waves (glossary, people, money, video ASR…) = optional checkboxes inside a dig-leaf, not a second constitution.

---

## Commands (correct venv)

```text
mag.cmd doctor
mag.cmd context-pack
mag.cmd bonds
mag.cmd trail start "goal" --seat local

mycelia.cmd x-pull …
mycelia.cmd thread-pull …
```

Bare PATH `python` is often Hermes — do not use it for Mag/mycelia.

---

## Depth drawers (open only when needed)

| Drawer | Doc |
|--------|-----|
| Cold vertex / SessionEnd | `docs/DNA.md` |
| Presented law | `docs/ref/MIRROR_PRESENTED.md` |
| Verkle × field memory | `docs/ref/memory_verkle_map.md` |
| Multi-agent / Elias rope | `docs/ref/COORDINATION_ELIAS_ROPE.md` |
| Run × trail lattice | `docs/ref/run_trail_lattice.md` |
| Story tab (thesis / journey) | Mag dock **Story** · `mag/story.py` · `memory/story/` |
| Mag OS / dashboard lore | `docs/ref/MAG_OS_v2.md` |
| Improve / compose | `docs/templates/FEATURE_COMPOSE.md` |
| Full card (lab) | `docs/ref/MAG_Card.md` |

---

## One line

**FIND truth as presented · FILE a bead · LOAD pack-first on a Verkle spine.**
