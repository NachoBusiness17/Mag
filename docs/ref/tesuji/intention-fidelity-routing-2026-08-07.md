# intention-fidelity-routing — steal tesuji

**schema:** `steal_tesuji.v1`  
**date:** 2026-08-07  
**commitment:** `steal-intention-fidelity-001`  
**status:** **taken**  
**in Mag:** `mag/intention_brief.py` · `mag/voice_turn.py` · `mag/router.py` (conversation depth) · Voice knot canvas

---

## 1. Origin (as presented)

| Field | Value |
|-------|--------|
| Names | Cursor swarm economics; Anthropic planner→generator→evaluator harness; Grok/ChatGPT always-frontier voice (contrast) |
| Host environments | Cursor Cloud / IDE; Anthropic cloud sessions; OpenAI/xAI voice products |
| Primary refs | cursor.com agent-swarm economics · Anthropic long-running harness essays · Mag `MAG_MOBILE_VOICE_SPEC` |
| Host wants | You rent frontier every turn, or live inside their session memory |

## 2. Contract we stole (invariant)

`IF smarter seats receive raw chat sludge / sticky desk goals instead of a compiled intention brief, THEN frontier compute is wasted and fidelity collapses to monologue.`

**Mechanism:** Cheap systems + local model **compile** `intention_brief.v1` (goal · constraints · depth · seat · why · context_refs). Smart seats fire **once** on that brief. Default mode **A**: local speaks; DeepSeek scouts dig board for the next turn.

## 3. Same as our world

- Pack-first / residual DNA / Verkle tip already law  
- `classify_depth` + seats already exist for dispatch  
- Voice dig board + shadow scout already run beside talk  
- Hardware envelope: voice stays gemma:2b  

## 4. Differs (why not just use the host)

- We refuse always-frontier as default (interesting as quality bar only)  
- Continuity is disk (tip/residual), not vendor chat  
- Home box routes; phone is thin client later  

## 5. What we like

- Prep before generate (planner→generator shape)  
- Cheap workers + scarce frontier (swarm economics)  
- Visible route (why this seat) on canvas  
- Topic reset so latest intention wins  

## 6. What we love

The tesuji is tiny: **structure intention with cheap compute, spend expensive compute on the structure.**  
Always-frontier sells unlimited *inference*; Mag sells unlimited *path* with sharp shots.

## 7. Why grateful we don’t live in the host environment

| Host path | What we’d lose |
|-----------|----------------|
| Always-frontier voice | Cost curve + sludge risk |
| Their session as memory | Residual / Verkle DNA |
| IDE-only swarm | Walk phone / Office as window |

## 8. Where it lives inside us

| Layer | Path |
|-------|------|
| Compiler | `mag/intention_brief.py` |
| Voice | `mag/voice_turn.py` consumes brief |
| Depth | `conversation` in `VALID_DEPTHS` / `classify_depth` |
| Canvas | `/api/v1/voice/canvas` + cast-voice knot |
| Law | this leaf · `HOME_HARDWARE_ENVELOPE` · mobile voice spec |

## 9. Compose

| Prior failure | Fix |
|---------------|-----|
| SAM monologue after “life and Bernays” | brief.goal + topic_reset + no sludge inject |
| Talk classified as simple_code | `conversation` depth |
| Escalate with empty prep | smart seat gets rendered brief + tip |

## 10. Adjacent quarries

- confidence-gated local-then-elevate  
- streaming TTS sentence boundary  
- walk seal → residual  
- Tailscale device token  

## 11. One line

**Compile intention cheaply; fire smart once; FILE the rest.**
