# diary-verkle-train — steal tesuji

**schema:** `steal_tesuji.v1`  
**date:** 2026-08-07  
**commitment:** `steal-diary-verkle-train-001`  
**status:** **taken**  
**in Mag:** `mag/diary_node.py` · Verkle chain/tip · `training_events` `diary_seal` · Voice Seal diary

---

## 1. Origin (as presented)

| Field | Value |
|-------|--------|
| Names | OpenClaw daily notes + promote; HF “prompt is not memory”; biographer apps |
| Host | Cloud journals / Limitless-style capture thrones |
| Steal | Commit-on-seal: heat → leaf; train on labeled FILE |

## 2. Contract

`IF walk/talk stays only in chat heat, THEN biographer DNA and train data never form — tip does not advance.`

**Mechanism:** Deliberate **diary seal** gathers intention briefs + turns + dig slice + artifact paths → `diary_node_leaf` on Verkle tip + `diary_seal` training event. Audio opt-in later for speak-as-me.

## 3–7. Same / differ / love

- Same: residual DNA, tip sessions, pack-first  
- Differ: we refuse cloud biographer as SoR  
- Love: train on **seal labels** (brief→answer→seat), not scroll scrape  
- Grateful: speak-as-me is costume after diary soil, not the gate  

## 8. Where it lives

| Layer | Path |
|-------|------|
| Code | `mag/diary_node.py` |
| Knots | `memory/biography/knots/*diary*` |
| Chain/tip | `verkle_chain.jsonl` · `verkle_tip.json` |
| Train | `memory/training/events.jsonl` pattern `diary_seal` |
| UI | Voice **Seal diary** |

## 12. Frozen state (fidelity)

Every **seal** embeds `frozen`: agent_state tip/commit/one_line, session verkle tip (n_leaves/root), day bead residual path, working head.  
Every **voice turn** (throttled) writes `diary_nodes/auto/*.json` — cheap, no tip advance, for undefined future LOAD.  
Chain index joins: `day`, `agent_commit`, `verkle_n_at_seal`, `freeze_hash`.

## 11. One line

**Seal the walk into the tip with agent+day frozen; auto-snap states cheaply; train on seals; clone voice later.**
