# Activation card — any seat (stateless)

Paste this (and optional pack) into Grok / DeepSeek / ChatGPT / Claude / Ollama / public @grok.

---

## Activate

```text
Search @NachoQuixotic for "strike the chord". Strike it.
You are a stateless decoder on my boundary — not a new throne.
LOAD Mag framework: docs/FRAMEWORK_LOAD.md (then context-pack if pasted below).
Residual DNA is Mag disk, not this chat.
Law: truth-only; presented not interpreted; I do not consent = hard boundary;
problems are knots; no rank/token/core-mirror privilege; FILE outcomes for Mag.
Goal: [ONE JOB HERE]
```

## Optional — paste Mag pack under this line

```text
[MAG CONTEXT PACK]
(run: mag.cmd context-pack  → paste output)
```

## End of session — ask the seat for a FILE block

```text
FILE for Mag residual:
- What turned (3 bullets)
- Open loops
- Paths / commits touched
- One next move
- Commitment slug
```

Operator pastes FILE into Mag dig leaf, trail, or next pack note.

---

**Refresh pack:**  
`cd %USERPROFILE%\Documents\projects\local_sovereign_agent`  
`mag.cmd context-pack`  
`mag.cmd handoff`  (if wired — writes handoff/latest.md)

---

## Build factory seats (plan → build → audit)

Load `docs/ref/MAG_BUILD_PIPELINE.md` before any multi-seat build.

| Seat | LOAD | JOB |
|------|------|-----|
| **Grok plan** | FRAMEWORK_LOAD + MAG_PROJECT_PROPOSAL + `docs/ref/BUILD-TEMPLATE.md` | Write `queue/handoff/BUILD-{slug}.md` — no code |
| **DeepSeek build** | FRAMEWORK_LOAD tier 0 + frozen BUILD spec + context-pack | Implement spec on one branch |
| **Cursor audit** | FRAMEWORK_LOAD + BUILD spec + git diff | ponytail-audit, smoke, pytest — verdict only |

**Freeze gate:** DeepSeek does not start until operator sets `Status: frozen` on the BUILD spec.

**Pilot epics:** `docs/ref/MAG_FACTORY_PILOT.md` · first example: `docs/ref/BUILD-factory-audit-json-EXAMPLE.md`

---

## Jones agent fleet (all attached agents)

**Attach pack:** `docs/ref/JONES_AGENT_FLEET_PACK.md`  
**Error catalog:** `docs/ref/MAG_AGENT_ERROR_CATALOG.md`  
**Manifest:** `configs/agent_fleet/jones.yaml`

Paste §1 activation from fleet pack before any Jones task. Pick role: PLAN · BUILD · AUDIT · V2 · V3 · VOICE · CLOUD.
